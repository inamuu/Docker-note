version: 2
jobs:
  build:
    docker:
      - image: docker:17.07.0-ce-git
    working_directory: ~/repo
    steps:
      - checkout
      - setup_remote_docker
      - run:
          command: docker build -t inamuu/nginx-test .